/** Bottom toolbar links — add a route under `app/<segment>/page.tsx` when extending. */
export const MODEL_VARIANT_NAV = [
  { href: "/", label: "Original" },
  { href: "/opus", label: "Opus" },
  { href: "/gpt", label: "GPT" },
] as const;
